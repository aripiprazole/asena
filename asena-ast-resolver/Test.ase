use IO
use Nat

enum Vec (a: Set) (n: Nat) where Monad Nat {
  nil  : Vec a 0,
  cons : (head: a) -> (tail: Vec a n) -> Vec a (n + 1),
}

Main {
  let x = match y {
    Vec.cons x xs -> { Vec.cons x xs },
    Vec.nil       -> { Vec.nil },
  }
  let Vec.nil = Vec.cons (Vec.cons Vec.nil)
  IO.println "hello, world"
}
