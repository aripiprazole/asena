use IO
use Nat

enum Vec (a: Set) (n: Nat) {
  nil  : Vec a 0,
  cons : (head: a) -> (tail: Vec a n) -> Vec a (n + 1),
}

Main {
  let y = Vec.cons 10 Vec.nil
  let a = match afka {
    Vec.cons x xs => Vec.cons x xs,
    Vec.nil => Vec.nil
  }
  let Vec.nil = Vec.cons a
  IO.println "hello, world"
}
