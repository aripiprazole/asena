name: Option String;

enum Maybe[a] {
    Just(a),
    Nothing,

    fun unwrap(self) {
        println "hello"
    }
}

IO.println(message: String): Unit {
    let x = 10
    let y = x + y in
    x + y
};

Main {
    IO.println (Option.unwrap name)
}
